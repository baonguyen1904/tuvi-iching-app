---
phase: 01-data-pipeline-scoring-engine
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - backend/src/scrapers/cohoc_scraper.py
  - backend/tests/test_cohoc_scraper.py
autonomous: true
requirements: [DATA-01, DATA-03]
must_haves:
  truths:
    - "parse_body(html) returns a dict with keys: nam, menh, cuc, than_cu, menh_chu, than_chu, am_duong, cung (list of 12), cung_10yrs (list of 12)"
    - "Each cung entry has keys: ten (str), sao (list of strings)"
    - "parse_body handles both Dương Nam/Âm Nữ ordering via main_order and reversed_order arrays"
    - "get_gio_sinh_option(time) returns correct canh giờ index 1-12 for any hour"
    - "Unit tests for parse_body using fixture HTML pass without Selenium"
  artifacts:
    - path: "backend/src/scrapers/cohoc_scraper.py"
      provides: "get_page_detail(), parse_body(), get_gio_sinh_option()"
      exports: [get_page_detail, parse_body, get_gio_sinh_option]
    - path: "backend/tests/test_cohoc_scraper.py"
      provides: "Unit tests for parse_body using fixture HTML"
      contains: "def test_"
  key_links:
    - from: "backend/src/scrapers/cohoc_scraper.py"
      to: "backend/src/soup_utils.py"
      via: "from src.soup_utils import find_metadata_field"
      pattern: "from src\\.soup_utils import"
    - from: "backend/src/scrapers/cohoc_scraper.py"
      to: "backend/src/constants.py"
      via: "from src.constants import LUNA_YEARS, GIO_SINH"
      pattern: "from src\\.constants import"
---

<objective>
Port the tuvi.cohoc.net Selenium scraper from the legacy codebase to the new backend project, adapting imports and adding unit tests for the HTML parsing logic.

Purpose: DATA-01 — given birth data, scrape cohoc.net and return parsed lá số with 12 cung × sao for cả đời and 10 năm views.
Output: backend/src/scrapers/cohoc_scraper.py with get_page_detail() and parse_body(), plus unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-pipeline-scoring-engine/01-CONTEXT.md
@.planning/phases/01-data-pipeline-scoring-engine/01-01-SUMMARY.md

<interfaces>
<!-- Legacy scraper to port — read this file entirely before writing anything: -->
<!-- /mnt/d/Working/TBTesu/tuvi/scraper/src/scrapers/tuvi_cohoc_scraper.py -->

<!-- Key patterns in the legacy scraper: -->
<!-- URL = "http://tuvi.cohoc.net/lay-la-so-tu-vi-ngay-duong-lich.html" -->
<!-- parse_body(body) returns dict with cung and cung_10yrs lists -->
<!-- main_order = [0, 1, 2, 3, 5, 7, 11, 10, 9, 8, 6, 4] -->
<!-- reversed_order = [0, 4, 6, 8, 9, 10, 11, 7, 5, 3, 2, 1] -->
<!-- Dương Nam / Âm Nữ → main_order; others → reversed_order -->
<!-- cung_tds = soup.select("td.cung") -->
<!-- start = "MỆNH" (uppercase in cohoc.net) -->
<!-- chinh_tinh: "div.cung-top > div.chinh-tinh > p:nth-child(1) > span" -->
<!-- sao_tot: "div.cung-middle > ul.sao-tot > li" -->
<!-- sao_xau: "div.cung-middle > ul.sao-xau > li" -->
<!-- sao_bottom: "div.cung-bottom > p > span" -->
<!-- vận 10 năm: span.cung-tieuvan contains luna year string like "Ngọ" -->

<!-- Driver creation (from legacy main.py): -->
import undetected_chromedriver as uc
options = uc.ChromeOptions()
driver = uc.Chrome(options=options)
driver.set_window_size(694, 1080)

<!-- Schemas available from 01-01: -->
from src.schemas import CungDetail, CungInfo, LaSoTuVi, LaSoTuViInput
from src.constants import LUNA_YEARS, GIO_SINH
from src.soup_utils import find_metadata_field
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port cohoc_scraper.py with adapted imports</name>
  <files>backend/src/scrapers/cohoc_scraper.py</files>
  <read_first>
    - /mnt/d/Working/TBTesu/tuvi/scraper/src/scrapers/tuvi_cohoc_scraper.py (full legacy file to port)
    - /mnt/d/Working/TBTesu/tuvi/scraper/src/constants.py (constants that were imported in legacy)
    - backend/src/schemas.py (confirm CungDetail, CungInfo field names)
    - backend/src/constants.py (confirm LUNA_YEARS, GIO_SINH are available)
  </read_first>
  <action>
    Port `tuvi_cohoc_scraper.py` to `backend/src/scrapers/cohoc_scraper.py`. Changes from legacy:

    1. **Imports**: Replace `import constants` → `from src.constants import LUNA_YEARS, GIO_SINH`; Replace `import soup_utils` → `from src.soup_utils import find_metadata_field`; Remove `import schemas` (not needed in this file)

    2. **Remove `get_all()` function** — bulk scraping utility not needed for MVP.

    3. **Keep ALL of these functions exactly**:
       - `get_gio_sinh_option(time: datetime) -> int` — returns canh giờ index 1-12
       - `input_user_info(driver, dob, time, gender)` — fills form on cohoc.net
       - `get_list_cungs(cung_tds) -> list` — extracts cung names from td elements
       - `parse_body(body: str) -> dict` — parses HTML, returns `{nam, menh, cuc, than_cu, menh_chu, than_chu, am_duong, cung: List[dict], cung_10yrs: List[dict]}`
       - `get_page_detail(driver, dob, time, gender) -> dict` — Selenium automation

    4. **parse_body output format**: Keep the raw dict format `{"ten": ..., "sao": [...]}` per cung. Do NOT convert to CungDetail Pydantic models here — the FastAPI endpoint (Plan 05) will do that.

    5. **Add create_driver() function** (from legacy main.py):
    ```python
    def create_driver():
        import undetected_chromedriver as uc
        from src.constants import CHROME_PATH
        options = uc.ChromeOptions()
        options.add_argument("--headless=new")
        options.add_argument("--no-sandbox")
        options.add_argument("--disable-dev-shm-usage")
        driver = uc.Chrome(options=options)
        driver.set_window_size(694, 1080)
        return driver
    ```

    6. **URL constant**: `URL = "http://tuvi.cohoc.net/lay-la-so-tu-vi-ngay-duong-lich.html"`

    7. **Keep all error handling**: IP rate-limit detection (`ip-deny` / `no-refer-ip-deny` in URL), processing page detection (`cache-not-found`), 90s wait for server cache generation.
  </action>
  <verify>
    <automated>cd /mnt/d/Working/TBTesu/TuViApp/backend && uv run python -c "from src.scrapers.cohoc_scraper import parse_body, get_gio_sinh_option, get_page_detail, create_driver; print('imports OK')"</automated>
  </verify>
  <done>
    - backend/src/scrapers/cohoc_scraper.py imports from src.constants, src.soup_utils (not from legacy paths)
    - parse_body, get_gio_sinh_option, get_page_detail, create_driver all importable
    - No reference to Windows paths (D:\Working\...) anywhere in the file
  </done>
  <acceptance_criteria>
    - backend/src/scrapers/cohoc_scraper.py contains `def parse_body`
    - backend/src/scrapers/cohoc_scraper.py contains `def get_page_detail`
    - backend/src/scrapers/cohoc_scraper.py contains `def get_gio_sinh_option`
    - backend/src/scrapers/cohoc_scraper.py contains `def create_driver`
    - backend/src/scrapers/cohoc_scraper.py contains `from src.constants import`
    - backend/src/scrapers/cohoc_scraper.py contains `from src.soup_utils import`
    - backend/src/scrapers/cohoc_scraper.py does NOT contain `D:\\Working`
    - `uv run python -c "from src.scrapers.cohoc_scraper import parse_body"` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for parse_body using fixture HTML</name>
  <files>backend/tests/test_cohoc_scraper.py, backend/tests/fixtures/cohoc_sample.html</files>
  <read_first>
    - backend/src/scrapers/cohoc_scraper.py (understand what parse_body returns to write correct assertions)
    - /mnt/d/Working/TBTesu/tuvi/scraper/src/scrapers/tuvi_cohoc_scraper.py (see parse_body return structure)
    - backend/src/schemas.py (CungDetail fields to verify against)
  </read_first>
  <behavior>
    - test_parse_body_returns_12_cungs: parse_body(sample_html) returns dict with "cung" list of length 12
    - test_parse_body_returns_12_10yrs: parse_body(sample_html) returns dict with "cung_10yrs" list of length 12
    - test_parse_body_has_metadata: result["am_duong"] is not None and result["menh"] is not None
    - test_parse_body_cung_has_ten_and_sao: result["cung"][0] has "ten" and "sao" keys, sao is a list
    - test_get_gio_sinh_option_midnight: datetime with hour=23 returns 1
    - test_get_gio_sinh_option_morning: datetime with hour=8 returns 5 (7-9 range)
    - test_get_gio_sinh_option_noon: datetime with hour=12 returns 7 (11-13 range)
  </behavior>
  <action>
    **Step 1**: Create a minimal fixture HTML file at `backend/tests/fixtures/cohoc_sample.html`.

    The fixture must contain enough HTML structure for parse_body to work:
    - A `div.thien-ban` with metadata (Năm:, Mệnh:, Cục:, Thân cư:, Mệnh chủ:, Thân chủ:, Âm Dương:)
    - Exactly 12 `td.cung` elements, each with:
      - `p.cung-tencung` containing a cung name (include "MỆNH" for start_idx detection)
      - `div.cung-top > div.chinh-tinh > p:nth-child(1) > span` (chinh tinh star)
      - `div.cung-middle > ul.sao-tot > li` (1-3 stars)
      - `div.cung-middle > ul.sao-xau > li` (0-2 stars)
      - `div.cung-bottom > p > span` (bottom star)
      - `span.cung-tieuvan` (luna year, e.g. "Ngọ")
    - Set `am_duong` to "Dương Nam" in the fixture

    Cung names for fixture (in HTML order, positions 0-11):
    `MỆNH, Phụ Mẫu, Phúc Đức, Điền Trạch, Quan Lộc, Nô Bộc, Thiên Di, Tật Ách, Tài Bạch, Tử Tức, Huynh Đệ, Phu Thê`
    Luna years for span.cung-tieuvan: Tí, Sửu, Dần, Mão, Thìn, Tị, Ngọ, Mùi, Thân, Dậu, Tuất, Hợi

    **Step 2**: Write tests in `backend/tests/test_cohoc_scraper.py`:
    ```python
    from pathlib import Path
    from datetime import datetime
    from src.scrapers.cohoc_scraper import parse_body, get_gio_sinh_option

    FIXTURE_HTML = (Path(__file__).parent / "fixtures" / "cohoc_sample.html").read_text(encoding="utf-8")

    def test_parse_body_returns_12_cungs(): ...
    def test_parse_body_returns_12_10yrs(): ...
    def test_parse_body_has_metadata(): ...
    def test_parse_body_cung_has_ten_and_sao(): ...
    def test_get_gio_sinh_option_midnight(): ...
    def test_get_gio_sinh_option_morning(): ...
    def test_get_gio_sinh_option_noon(): ...
    ```

    Run tests — they should fail. Fix the fixture if parse_body raises errors, then re-run until green.
  </action>
  <verify>
    <automated>cd /mnt/d/Working/TBTesu/TuViApp/backend && uv run pytest tests/test_cohoc_scraper.py -v --tb=short</automated>
  </verify>
  <done>
    - backend/tests/fixtures/cohoc_sample.html contains valid cohoc.net-style HTML with 12 td.cung
    - All 7 test cases in test_cohoc_scraper.py pass
    - Tests run without Selenium (parse_body only, no browser)
  </done>
  <acceptance_criteria>
    - backend/tests/fixtures/cohoc_sample.html exists
    - backend/tests/fixtures/cohoc_sample.html contains `td class="cung"` (12 occurrences)
    - backend/tests/fixtures/cohoc_sample.html contains `MỆNH`
    - backend/tests/test_cohoc_scraper.py contains `def test_parse_body_returns_12_cungs`
    - backend/tests/test_cohoc_scraper.py contains `def test_get_gio_sinh_option`
    - `uv run pytest tests/test_cohoc_scraper.py -v` exits 0
    - `uv run pytest tests/test_cohoc_scraper.py -v` output contains "7 passed" or more
  </acceptance_criteria>
</task>

</tasks>

<verification>
cd /mnt/d/Working/TBTesu/TuViApp/backend && uv run pytest tests/test_cohoc_scraper.py tests/test_schemas.py -v
</verification>

<success_criteria>
- cohoc_scraper.py is a complete port of the legacy scraper with no Windows paths
- parse_body correctly handles Dương Nam/Âm Nữ vs Âm Nam/Dương Nữ cung ordering
- All HTML parsing tests pass using fixture HTML (no Selenium dependency in tests)
- get_gio_sinh_option correctly maps any hour to canh giờ 1-12
- IP rate-limit and processing-page error handling preserved from legacy
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-pipeline-scoring-engine/01-03-SUMMARY.md`
</output>
