---
phase: 01-data-pipeline-scoring-engine
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - backend/src/scrapers/tuvivn_scraper.py
  - backend/tests/test_tuvivn_scraper.py
  - backend/tests/fixtures/tuvivn_sample.html
autonomous: true
requirements: [DATA-02, DATA-03]
must_haves:
  truths:
    - "parse_body(html) returns a dict with 'cung' list of 12 CungDetail-compatible dicts, each with ten, sao, thang"
    - "thang field contains values like 'Th.1' through 'Th.12'"
    - "sort_cung_by_month() returns cungs ordered by thang month number ascending"
    - "d-none hidden stars are excluded from sao lists"
    - "Unit tests for parse_body using fixture HTML pass without Selenium"
  artifacts:
    - path: "backend/src/scrapers/tuvivn_scraper.py"
      provides: "get_page_detail(), parse_body(), sort_cung_by_month()"
      exports: [get_page_detail, parse_body, sort_cung_by_month]
    - path: "backend/tests/test_tuvivn_scraper.py"
      provides: "Unit tests for parse_body using fixture HTML"
      contains: "def test_"
  key_links:
    - from: "backend/src/scrapers/tuvivn_scraper.py"
      to: "backend/src/schemas.py"
      via: "from src.schemas import CungDetail, CungInfo, LaSoTuVi"
      pattern: "from src\\.schemas import"
    - from: "backend/src/scrapers/tuvivn_scraper.py"
      to: "backend/src/soup_utils.py"
      via: "from src.soup_utils import find_metadata_field"
      pattern: "from src\\.soup_utils import"
---

<objective>
Port the tuvi.vn Selenium scraper from the legacy codebase to the new backend project, adapting imports and adding unit tests for the HTML parsing logic.

Purpose: DATA-02 — given birth data, scrape tuvi.vn and return parsed lá số with 12 cung × sao for 12 tháng monthly view.
Output: backend/src/scrapers/tuvivn_scraper.py with get_page_detail() and parse_body(), plus unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-pipeline-scoring-engine/01-CONTEXT.md
@.planning/phases/01-data-pipeline-scoring-engine/01-01-SUMMARY.md

<interfaces>
<!-- Legacy scraper to port — read this file entirely before writing anything: -->
<!-- /mnt/d/Working/TBTesu/tuvi/scraper/src/scrapers/tuvi_vn_scraper.py -->

<!-- Key patterns in the legacy scraper: -->
<!-- URL = "https://tuvi.vn/lap-la-so-tu-vi" -->
<!-- parse_body(body) returns dict with "cung" list of CungDetail objects (with thang) -->
<!-- main_order = [0, 1, 2, 3, 5, 7, 11, 10, 9, 8, 6, 4] -->
<!-- reversed_order = [0, 4, 6, 8, 9, 10, 11, 7, 5, 3, 2, 1] -->
<!-- start = unicodedata.normalize("NFC", "Mệnh") (NFC normalized, not uppercase MỆNH) -->
<!-- cung name: p.text-sao-chinh-tinh -->
<!-- thang: div.view-cung-dai-van > p (second p element, e.g. "Th.1") -->
<!-- chinh_tinh_els: div.chinh-tinh p.text-chinh-chinh -->
<!-- sao_tot: div.sao-tot > div[data-sao-id] (filter out d-none class) -->
<!-- sao_xau: div.sao-xau > div[data-sao-id] (filter out d-none class) -->
<!-- bottom_el: div.cung-bottom > span.txt-tiny-mid -->
<!-- to_string() helper: NFC normalize, strip whitespace, remove - and + chars -->

<!-- Key difference from cohoc: tuvi.vn uses "Mệnh" (mixed case, NFC) not "MỆNH" (uppercase) -->
<!-- Key difference: thang is extracted from div.view-cung-dai-van, not present in cohoc -->
<!-- Key difference: d-none filtering needed for hidden star elements -->

<!-- Schemas available from 01-01: -->
from src.schemas import CungDetail, CungInfo, LaSoTuVi, LaSoTuViInput
from src.soup_utils import find_metadata_field
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port tuvivn_scraper.py with adapted imports</name>
  <files>backend/src/scrapers/tuvivn_scraper.py</files>
  <read_first>
    - /mnt/d/Working/TBTesu/tuvi/scraper/src/scrapers/tuvi_vn_scraper.py (full legacy file to port)
    - backend/src/schemas.py (CungDetail, CungInfo, LaSoTuVi types)
    - backend/src/scrapers/cohoc_scraper.py (see create_driver pattern already established)
  </read_first>
  <action>
    Port `tuvi_vn_scraper.py` to `backend/src/scrapers/tuvivn_scraper.py`. Changes from legacy:

    1. **Imports**: Replace `import schemas` → `from src.schemas import CungDetail, CungInfo, LaSoTuVi`; Replace `import soup_utils` → `from src.soup_utils import find_metadata_field`; Remove `import constants` (not used here)

    2. **Keep ALL of these functions exactly**:
       - `to_string(text: str) -> str` — NFC normalize, strip, remove `-` and `+`
       - `parse_body(body: str) -> dict` — parses HTML, returns `{nam, menh, cuc, menh_chu, than_chu, am_duong, cung: List[CungDetail]}`
       - `sort_cung_by_month(cung: List[CungDetail]) -> List[CungDetail]` — sorts by thang month number
       - `input_user_info(driver, dob, gender, full_name, nam_xem)` — fills form on tuvi.vn
       - `get_page_detail(driver, dob, gender, full_name, nam_xem) -> LaSoTuVi` — Selenium automation

    3. **parse_body must return CungDetail objects** (not raw dicts), because the legacy code already creates `schemas.CungDetail(ten=..., sao=..., thang=...)`. Port this unchanged.

    4. **get_page_detail must return a LaSoTuVi object** with:
       ```python
       cung=CungInfo(
           cung_chung=data["cung"],   # same data, full view
           cung_10yrs=[],              # tuvi.vn doesn't provide 10-year view
           cung_12months=sort_cung_by_month(data["cung"]),
       )
       ```

    5. **Remove get_la_so_url()** — the POST-without-Selenium approach is unreliable, not needed.

    6. **Add create_driver() import**: Do NOT define create_driver() here — import it from cohoc_scraper:
       ```python
       from src.scrapers.cohoc_scraper import create_driver
       ```

    7. **URL constant**: `URL = "https://tuvi.vn/lap-la-so-tu-vi"`

    8. **d-none filtering**: Keep the check `if "d-none" not in classes` for sao_tot and sao_xau divs.

    9. **nam_xem handling**: When nam_xem is None, do not attempt to set the viewYear select.
  </action>
  <verify>
    <automated>cd /mnt/d/Working/TBTesu/TuViApp/backend && uv run python -c "from src.scrapers.tuvivn_scraper import parse_body, sort_cung_by_month, get_page_detail; print('imports OK')"</automated>
  </verify>
  <done>
    - backend/src/scrapers/tuvivn_scraper.py imports from src.schemas, src.soup_utils
    - parse_body, sort_cung_by_month, get_page_detail all importable
    - No reference to Windows paths or legacy `import schemas` pattern
  </done>
  <acceptance_criteria>
    - backend/src/scrapers/tuvivn_scraper.py contains `def parse_body`
    - backend/src/scrapers/tuvivn_scraper.py contains `def get_page_detail`
    - backend/src/scrapers/tuvivn_scraper.py contains `def sort_cung_by_month`
    - backend/src/scrapers/tuvivn_scraper.py contains `def to_string`
    - backend/src/scrapers/tuvivn_scraper.py contains `from src.schemas import`
    - backend/src/scrapers/tuvivn_scraper.py contains `from src.soup_utils import`
    - backend/src/scrapers/tuvivn_scraper.py does NOT contain `import schemas` (bare import)
    - `uv run python -c "from src.scrapers.tuvivn_scraper import parse_body"` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for parse_body using fixture HTML</name>
  <files>backend/tests/test_tuvivn_scraper.py, backend/tests/fixtures/tuvivn_sample.html</files>
  <read_first>
    - backend/src/scrapers/tuvivn_scraper.py (understand parse_body return structure)
    - /mnt/d/Working/TBTesu/tuvi/scraper/src/scrapers/tuvi_vn_scraper.py (see HTML selectors used)
    - backend/tests/fixtures/cohoc_sample.html (see how a fixture is structured from Plan 03)
  </read_first>
  <behavior>
    - test_parse_body_returns_12_cungs: parse_body(sample_html) returns dict with "cung" list of length 12
    - test_parse_body_cung_has_thang: each cung in result["cung"] has .thang attribute (not None)
    - test_parse_body_thang_format: cung[0].thang matches "Th.\d+" pattern
    - test_sort_cung_by_month_order: sort_cung_by_month([Th.3, Th.1, Th.2]) returns [Th.1, Th.2, Th.3]
    - test_parse_body_excludes_dnone: stars with d-none class are NOT in any cung's sao list
    - test_parse_body_has_menh_cung: at least one cung has .ten == "Mệnh"
  </behavior>
  <action>
    **Step 1**: Create `backend/tests/fixtures/tuvivn_sample.html` with tuvi.vn-style HTML structure:

    Outer container: `div.view-thien-ban`
    Metadata table: `<table>` with `<td>` pairs for Năm:, Bản mệnh:, Chủ mệnh:, Chủ thân:, Âm dương:
    12 `td.cung` elements, each with:
    - `p.text-sao-chinh-tinh` containing NFC-normalized cung name (e.g. "Mệnh", "Quan Lộc")
    - `div.view-cung-dai-van` with two `<p>` elements: first = decade label, second = thang (e.g. "Th.1")
    - `div.chinh-tinh` with `p.text-chinh-chinh` containing a main star name
    - `div.sao-tot` with `div[data-sao-id]` elements (one normal, one with class="d-none")
    - `div.sao-xau` with `div[data-sao-id]` elements
    - `div.cung-bottom` with `span.txt-tiny-mid` containing a bottom star name

    Cung names: Mệnh, Phụ Mẫu, Phúc Đức, Điền Trạch, Quan Lộc, Nô Bộc, Thiên Di, Tật Ách, Tài Bạch, Tử Tức, Huynh Đệ, Phu Thê
    Thang values: Th.1 through Th.12 (not in order — mix them up to test sorting)
    Set am_duong to "Dương Nam"

    **Step 2**: Write tests in `backend/tests/test_tuvivn_scraper.py`:
    ```python
    import re
    from pathlib import Path
    from src.scrapers.tuvivn_scraper import parse_body, sort_cung_by_month
    from src.schemas import CungDetail

    FIXTURE_HTML = (Path(__file__).parent / "fixtures" / "tuvivn_sample.html").read_text(encoding="utf-8")

    def test_parse_body_returns_12_cungs(): ...
    def test_parse_body_cung_has_thang(): ...
    def test_parse_body_thang_format(): ...
    def test_sort_cung_by_month_order(): ...
    def test_parse_body_excludes_dnone(): ...
    def test_parse_body_has_menh_cung(): ...
    ```
  </action>
  <verify>
    <automated>cd /mnt/d/Working/TBTesu/TuViApp/backend && uv run pytest tests/test_tuvivn_scraper.py -v --tb=short</automated>
  </verify>
  <done>
    - backend/tests/fixtures/tuvivn_sample.html contains valid tuvi.vn-style HTML with 12 td.cung
    - All 6 test cases pass
    - Tests run without Selenium
    - d-none filtering is verified by test
  </done>
  <acceptance_criteria>
    - backend/tests/fixtures/tuvivn_sample.html exists
    - backend/tests/fixtures/tuvivn_sample.html contains `text-sao-chinh-tinh`
    - backend/tests/fixtures/tuvivn_sample.html contains `d-none`
    - backend/tests/test_tuvivn_scraper.py contains `def test_parse_body_returns_12_cungs`
    - backend/tests/test_tuvivn_scraper.py contains `def test_parse_body_excludes_dnone`
    - `uv run pytest tests/test_tuvivn_scraper.py -v` exits 0
    - `uv run pytest tests/test_tuvivn_scraper.py -v` output contains "6 passed" or more
  </acceptance_criteria>
</task>

</tasks>

<verification>
cd /mnt/d/Working/TBTesu/TuViApp/backend && uv run pytest tests/test_tuvivn_scraper.py tests/test_cohoc_scraper.py -v
</verification>

<success_criteria>
- tuvivn_scraper.py is a complete port of legacy scraper with no Windows paths
- parse_body correctly identifies start cung "Mệnh" using NFC normalization
- d-none hidden stars are filtered out correctly
- thang field correctly populated per cung (Th.1 through Th.12)
- sort_cung_by_month sorts CungDetail list by month number ascending
- All HTML parsing tests pass using fixture HTML (no Selenium)
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-pipeline-scoring-engine/01-04-SUMMARY.md`
</output>
